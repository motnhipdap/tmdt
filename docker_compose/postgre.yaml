version: "3.8"
services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_DB=dungcony
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=123456
    ports:
      - "5432:5432"
